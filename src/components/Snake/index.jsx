import  * as React from 'react';

const Snake = ({ applePosition, direction, oldSnake, maxRow = 20, maxCol = 20 }) => {

  let horizontalDirections = ['l', 'r'];
  let verticalDirections = ['u', 'd'];

  const getNextSnake = () => {

  }

  return (

  )
}